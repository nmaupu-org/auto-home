---
image:
  repository: nmaupu/mqttgateway
  tag: latest
  pullPolicy: Always

deployment:
  extraArgs:
    - --log.level=info
    - --mqtt.topic=#
    - --mqtt.prefix=

serviceMonitor:
  enabled: true
  extraLabels:
    monitor: home

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: heimdall
  namespace: argocd
spec:
  project: default
  destination:
    namespace: heimdall
    server: https://kubernetes.default.svc
  sources:
    - chart: heimdall
      repoURL: oci.trueforge.org/truecharts
      targetRevision: 25.0.1
      helm:
        releaseName: heimdall
        valueFiles:
          - $values/k8s/argocd/home/tools/heimdall/values.yaml
    - repoURL: https://github.com/nmaupu-org/auto-home.git
      targetRevision: master
      ref: values
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jqPathExpressions:
        - .spec.template.spec.hostUsers
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netdata-iot
  namespace: argocd
spec:
  project: default
  destination:
    namespace: netdata
    server: https://kubernetes.default.svc
  sources:
    - path: k8s/charts/host-ingress
      repoURL: https://github.com/nmaupu-org/auto-home.git
      targetRevision: master
      helm:
        releaseName: netdata-iot
        valueFiles:
          - $values/k8s/argocd/home/monitoring/netdata/iot-values.yaml
    - repoURL: https://github.com/nmaupu-org/auto-home.git
      targetRevision: master
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
